Almost all used libs should be in node_modules folder.
To create it in the project run:

    npm install

Start the server

    node server.js
   
Karma with Jasmine are integrated for unit testing:
    
    karma start
